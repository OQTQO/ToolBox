# 当前任务

状态：已完成（2026-09-04）

## 任务

- 编号：2026-09-04-cross-repo-plugin-sdk-0-5-0
- 目标：以已发布 ToolBox v0.5.0 为权威基线，统一软件侧插件开发文档、版本说明和跨仓库交接记录，并同步插件仓库的 SDK 0.5.0 开发基线。
- 范围：软件仓库公开 README、CHANGELOG、插件安全文档和维护任务记录；插件仓库 SDK 引用、插件开发文档、兼容矩阵、贡献说明、恢复脚本和任务记录。
- 权威源：软件 v0.5.0 Release，标签提交 `9d1559e4df3da77b9911ae937580e73f85356e1c`；软件当前 `main` 提交 `d3c63af`。
- 变更基线：软件 `main` 原有的本机缓存忽略规则已移入本地 `.git/info/exclude`，不进入项目仓库；插件 `main` 已有本地提交 `713204a` 及 5 个未提交交接文档修改均保留，本任务在其上完成版本同步。
- 分支决定：保留两个仓库当前 `main`，分别提交并推送；用户已明确授权本次 GitHub 同步。
- 验收：软件文档不再把 v0.5 能力写成 v0.4；插件源码、测试和 CI 使用 SDK 0.5.0；恢复摘要显示版本一致；两仓库独立验证、提交和推送完成。

## 下一步

1. 用户和贡献者从 GitHub `main` 与 ToolBox v0.5.0 DevKit 开始插件开发。
2. 新的平台协议或插件发布需求另建任务。

## 已完成

- 软件 README、安全文档和 CHANGELOG 已统一到 v0.5.0，并明确插件开发使用已发布 SDK/DevKit。
- 保留平台 v0.5.0 Release 资产和历史 v0.4.0 记录，不修改历史归档。
- 软件仓库提交 `1b17153` 已推送到 GitHub `origin/main`。

## 验证

- `dotnet test ToolBox.sln --configuration Release`：Core 64/64、Host 42/42 通过；Host 测试使用既有 Release 输出执行。
- `git diff --check`：通过，仅有既有 LF/CRLF 转换提示。
- 插件仓库已完成 SDK 0.5.0 适配、严格构建、测试、确定性打包和跨仓库 Host 烟雾验证。
- 恢复上下文显示软件与插件 SDK 均为 0.5.0，版本漂移警告已消失。
- 插件仓库提交 `6032d08`（含此前的 `713204a`）已推送到 GitHub `origin/main`。
- 软件仓库跟踪文件工作区已清洁；本机 `.audit-obj/`、`.dotnet-cli-home/` 仅由 `.git/info/exclude` 忽略。

## 已知边界

- 本轮未重新生成软件正式 Release；v0.5.0 已发布资产不变。
- Windows 原生窗口视觉和真实用户验收仍以用户桌面环境为准。
- 本机审计/交接缓存不属于项目发布内容，保留在本地 Git exclude 中，不同步到 GitHub。
